# Deployment Flowchart

Visual guide to the deployment process.

---

## 🎯 Overall Deployment Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     START DEPLOYMENT                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: Create TiDB Cloud Account & Cluster               │
│  ⏱️ Time: 10 minutes                                        │
│  ✅ Output: Database credentials                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 2: Deploy Backend to Render                          │
│  ⏱️ Time: 15 minutes                                        │
│  ✅ Output: Backend URL                                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 3: Deploy Frontend to Vercel                         │
│  ⏱️ Time: 10 minutes                                        │
│  ✅ Output: Frontend URL                                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 4: Connect Frontend & Backend                        │
│  ⏱️ Time: 5 minutes                                         │
│  ✅ Output: Fully connected system                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 5: Test Everything                                   │
│  ⏱️ Time: 5 minutes                                         │
│  ✅ Output: Working application                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    🎉 SUCCESS!                              │
│              Your app is live and working!                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Detailed Step 1: TiDB Cloud

```
START
  │
  ▼
┌─────────────────────┐
│ Go to tidbcloud.com │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Sign Up/Login     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click Create Cluster│
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Select "Serverless" │
│    (Free Tier)      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Configure Cluster:  │
│ - Name              │
│ - Region            │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Click "Create"     │
│  Wait 2-3 minutes   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click "Connect"     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Generate Password   │
│ SAVE CREDENTIALS!   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Go to SQL Editor    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ CREATE DATABASE     │
│ school_portal;      │
└──────────┬──────────┘
           │
           ▼
✅ STEP 1 COMPLETE
   (Save credentials)
```

---

## 🔧 Detailed Step 2: Render Backend

```
START
  │
  ▼
┌─────────────────────┐
│ Push code to GitHub │
│ git push origin main│
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Go to render.com   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Sign Up with GitHub │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ New + → Web Service │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Select Repository   │
│   (SchoolPortal)    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Configure Service:  │
│ - Root: backend     │
│ - Build: npm install│
│ - Start: npm start  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Add 11 Environment  │
│    Variables        │
│ (Use TiDB creds)    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click "Create"      │
│ Wait 3-5 minutes    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ SAVE BACKEND URL!   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Run schema.sql in   │
│   TiDB SQL Editor   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Run seed-tidb.sql   │
│   in SQL Editor     │
└──────────┬──────────┘
           │
           ▼
✅ STEP 2 COMPLETE
   (Save backend URL)
```

---

## 🎨 Detailed Step 3: Vercel Frontend

```
START
  │
  ▼
┌─────────────────────┐
│  Go to vercel.com   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Sign Up with GitHub │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Add New → Project   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Import Repository   │
│   (SchoolPortal)    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Configure Project:  │
│ - Framework: Vite   │
│ - Root: frontend    │
│ - Build: auto       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Add Environment Var:│
│ VITE_API_URL=       │
│ [backend]/api/v1    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click "Deploy"      │
│ Wait 2-3 minutes    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ SAVE FRONTEND URL!  │
└──────────┬──────────┘
           │
           ▼
✅ STEP 3 COMPLETE
   (Save frontend URL)
```

---

## 🔗 Detailed Step 4: Connect Everything

```
START
  │
  ▼
┌─────────────────────┐
│ Go to Render        │
│   Dashboard         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Open Your Service   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click "Environment" │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Find FRONTEND_URL   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Update to Vercel URL│
│ (from Step 3)       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click "Save"        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Wait for Redeploy   │
│   (1-2 minutes)     │
└──────────┬──────────┘
           │
           ▼
✅ STEP 4 COMPLETE
   (Everything connected)
```

---

## ✅ Detailed Step 5: Testing

```
START
  │
  ▼
┌─────────────────────┐
│ Test Backend URL    │
│ in Browser          │
└──────────┬──────────┘
           │
           ├─ ✅ Works → Continue
           │
           └─ ❌ Fails → Check Render Logs
                         └─ Fix → Retry
           │
           ▼
┌─────────────────────┐
│ Test Frontend URL   │
│ in Browser          │
└──────────┬──────────┘
           │
           ├─ ✅ Loads → Continue
           │
           └─ ❌ Fails → Check Vercel Logs
                         └─ Fix → Retry
           │
           ▼
┌─────────────────────┐
│ Try Login           │
└──────────┬──────────┘
           │
           ├─ ✅ Success → Continue
           │
           └─ ❌ Fails → Check Console
                         └─ CORS? → Fix FRONTEND_URL
                         └─ 500? → Check Backend Logs
                         └─ Fix → Retry
           │
           ▼
┌─────────────────────┐
│ Check Dashboard     │
│ Loads Data?         │
└──────────┬──────────┘
           │
           ├─ ✅ Yes → Continue
           │
           └─ ❌ No → Check Database
                      └─ Run schema.sql
                      └─ Run seed-tidb.sql
                      └─ Retry
           │
           ▼
┌─────────────────────┐
│ Test CRUD           │
│ Operations          │
└──────────┬──────────┘
           │
           ├─ ✅ Works → Continue
           │
           └─ ❌ Fails → Check Logs
                         └─ Fix → Retry
           │
           ▼
┌─────────────────────┐
│ Check Console       │
│ for Errors          │
└──────────┬──────────┘
           │
           ├─ ✅ No Errors → SUCCESS!
           │
           └─ ❌ Has Errors → Fix → Retry
           │
           ▼
🎉 ALL TESTS PASSED!
   Your app is live!
```

---

## 🔄 Data Flow After Deployment

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Opens https://app.vercel.app
     ▼
┌─────────┐
│ Vercel  │ 2. Serves React app
└────┬────┘
     │
     │ 3. User clicks "Login"
     ▼
┌─────────┐
│ Browser │ 4. Sends POST to backend
└────┬────┘
     │
     │ 5. POST https://backend.onrender.com/api/v1/auth/login
     ▼
┌─────────┐
│ Render  │ 6. Validates credentials
└────┬────┘
     │
     │ 7. Query database
     ▼
┌─────────┐
│  TiDB   │ 8. Returns user data
└────┬────┘
     │
     │ 9. User found
     ▼
┌─────────┐
│ Render  │ 10. Generates JWT token
└────┬────┘
     │
     │ 11. Returns token + user info
     ▼
┌─────────┐
│ Browser │ 12. Stores token
└────┬────┘
     │
     │ 13. Redirects to dashboard
     ▼
┌─────────┐
│ Vercel  │ 14. Shows dashboard
└────┬────┘
     │
     │ 15. Fetches data with token
     ▼
┌─────────┐
│ Render  │ 16. Verifies token
└────┬────┘
     │
     │ 17. Queries database
     ▼
┌─────────┐
│  TiDB   │ 18. Returns data
└────┬────┘
     │
     │ 19. Data returned
     ▼
┌─────────┐
│ Browser │ 20. Displays data
└─────────┘
```

---

## 🚨 Error Handling Flow

```
Error Occurs
     │
     ▼
┌─────────────────────┐
│ Check Error Type    │
└────┬────────────────┘
     │
     ├─ CORS Error
     │  └─ Fix FRONTEND_URL in Render
     │     └─ Wait for redeploy
     │        └─ Test again
     │
     ├─ 500 Error
     │  └─ Check Render logs
     │     └─ Database issue?
     │        ├─ Yes → Check TiDB credentials
     │        └─ No → Check code error
     │           └─ Fix → Redeploy
     │
     ├─ Network Error
     │  └─ Check VITE_API_URL in Vercel
     │     └─ Correct? → Check backend is up
     │        └─ Fix → Redeploy
     │
     ├─ Build Error
     │  └─ Check build logs
     │     └─ Fix code error
     │        └─ Push to GitHub
     │           └─ Auto-redeploys
     │
     └─ Other Error
        └─ Check TROUBLESHOOTING.md
           └─ Follow specific guide
              └─ Fix → Test again
```

---

## 📊 Decision Tree: When to Upgrade

```
Is your app slow?
     │
     ├─ Yes → Is backend sleeping often?
     │        │
     │        ├─ Yes → Upgrade Render ($7/month)
     │        │
     │        └─ No → Is database slow?
     │                │
     │                ├─ Yes → Upgrade TiDB
     │                │
     │                └─ No → Optimize code
     │
     └─ No → Are you hitting limits?
              │
              ├─ Storage > 5GB → Upgrade TiDB
              │
              ├─ Requests > 50M/month → Upgrade TiDB
              │
              ├─ Bandwidth > 100GB/month → Upgrade Vercel
              │
              └─ No limits hit → Stay on free tier!
```

---

**Use these flowcharts to visualize and understand the deployment process!**
